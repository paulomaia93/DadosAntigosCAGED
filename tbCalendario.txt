
Criar Consulta (Data Mínima Manual):
Passos:
1. no Power Query cria uma consulta nova com o nome 
#date(2020, 01, 01) meta [IsParameterQuery=true, Type="Date", IsParameterQueryRequired=true]

Criar Consulta (Data Máxima Manual):
#date(2023, 8, 31) meta [IsParameterQuery=true, Type="Date", IsParameterQueryRequired=true]



Criar Consulta (dCalendario):

let
Fonte = 
    #"Data Máxima Manual"-#"Data Mínima Manual",
    #"Dias Extraídos" = Duration.Days(Fonte) + 1,
    #"Criar Tabela Calendário" = List.Dates(#"Data Mínima Manual",#"Dias Extraídos",#duration(1,0,0,0)),
    #"Convertido para Tabela" = Table.FromList(#"Criar Tabela Calendário", Splitter.SplitByNothing(), null, null, ExtraValues.Error),
    #"Colunas Renomeadas" = Table.RenameColumns(#"Convertido para Tabela",{{"Column1", "Data"}}),
    #"Tipo Alterado" = Table.TransformColumnTypes(#"Colunas Renomeadas",{{"Data", type date}}),
    #"Dia da Semana Inserido" = Table.AddColumn(#"Tipo Alterado", "Dia da Semana", each Date.DayOfWeek([Data]), Int64.Type),
    #"Nome do Dia Inserido" = Table.AddColumn(#"Dia da Semana Inserido", "Nome do Dia", each Date.DayOfWeekName([Data]), type text),
    #"Mês Inserido" = Table.AddColumn(#"Nome do Dia Inserido", "Mês", each Date.Month([Data]), Int64.Type),
    #"Nome do Mês Inserido" = Table.AddColumn(#"Mês Inserido", "Nome do Mês", each Date.MonthName([Data]), type text),
    #"Semana do Mês Inserida" = Table.AddColumn(#"Nome do Mês Inserido", "Semana do Mês", each Date.WeekOfMonth([Data]), Int64.Type),
    #"Ano Inserido" = Table.AddColumn(#"Semana do Mês Inserida", "Ano", each Date.Year([Data]), Int64.Type),
    #"Colocar Cada Palavra Em Maiúscula" = Table.TransformColumns(#"Ano Inserido",{{"Nome do Mês", Text.Proper, type text}, {"Nome do Dia", Text.Proper, type text}}),
    #"Primeiros caracteres extraídos" = Table.TransformColumns(#"Colocar Cada Palavra Em Maiúscula", {{"Nome do Dia", each Text.Start(_, 3), type text}}),
    #"Primeiros caracteres extraídos1" = Table.TransformColumns(#"Primeiros caracteres extraídos", {{"Nome do Mês", each Text.Start(_, 3), type text}})
in
    #"Primeiros caracteres extraídos1"